<?xml version="1.0"?>
<configuration>
  <appSettings>

    <add key="DisposableDbHostname" value="(local)" />
    <add key="DisposableDbConnString" value="Trusted_Connection=true;Min Pool Size=5;Max Pool Size=200;" />
    <add key="DbCreationPath" value="C:\SQLdata" />

    <add key="IgnoreViewPerformanceCheck+pcpx2" value="true" /><!-- This is pretty bad, pcpx2 has views which depend on other views, we should fix this... -->
    
  </appSettings>
</configuration>